const Loader = () => {
    return (
      <div className="loader">
        {/* Contenu du loader, par exemple un spinner */}
        <div className="spinner"></div>
      </div>
    );
  };
  
  export default Loader;